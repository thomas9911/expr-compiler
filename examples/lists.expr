# Use recursion + list_pop to count and sum list items
fn consume(xs, count, total):
    if list_len(xs) == 0:
        print(count)
        print(total)
        0
    else:
        x = list_pop(xs)
        consume(xs, count + 1, total + x)

fn main():
    xs = list_map(list_range(1, 5), fn x -> x * x end)
    consume(xs, 0, 0)
