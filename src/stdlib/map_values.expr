fn map_values(map) do
    __map_values_loop(map_iter(map), list_new())
end

fn __map_values_loop(it, acc) do
    if map_iter_done(it) do
        acc
    else
        key, value = map_iter_next(it)
        list_push(acc, value)
        __map_values_loop(it, acc)
    end
end

