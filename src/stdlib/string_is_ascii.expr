fn string_is_ascii(s) do
    __string_is_ascii_at(s, 0, bytes_len(s))
end

fn __string_is_ascii_at(s, index, len) do
    if index == len do
        true
    else
        bytes_get(s, index) < 128 and __string_is_ascii_at(s, index + 1, len)
    end
end
