fn string_ends_with(s, suffix) do
    __string_ends_with_len(s, suffix, bytes_len(s), bytes_len(suffix))
end

fn __string_ends_with_len(s, suffix, s_len, suffix_len) do
    suffix_len <= s_len and bytes_slice(s, s_len - suffix_len, s_len) == suffix
end
